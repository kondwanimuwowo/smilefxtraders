---
phase: 01-signal-language
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/(app)/cot/CotReports.tsx
autonomous: true
requirements: [PAGE-01]
user_setup: []

must_haves:
  truths:
    - "One pill row on /cot shows each pair with its SignalBars icon, plus an 'All' affordance"
    - "Tapping a pill filters the cards to that pair; the selected pill is visually obvious"
    - "The old summary pill strip and the separate pair filter tabs no longer render"
    - "The pill row wraps gracefully on narrow widths"
  artifacts:
    - "src/app/(app)/cot/CotReports.tsx (merged pill row replaces SummaryStrip render + filter-tabs render; SummaryStrip function removed)"
  key_links:
    - "The pill row reuses existing state: `pairs` (['All', ...entries]), `selected`, `setSelected`, and `visible` — no new state"
    - "Per-pair pills render <SignalBars signal={entry.signal} /> from Plan 01-01"
---

<objective>
Collapse the summary pill strip and the pair filter tabs into ONE interactive pill row on `/cot`. Each pill shows a pair with its SignalBars icon and acts as the filter; an "All" pill clears the filter.

Purpose: The old summary strip merely repeats what the cards say and duplicates the filter tabs — one merged, tappable pill row removes the repetition while keeping the filter.
Output: A single pill row in `CotReports.tsx`; `SummaryStrip` deleted; filter-tab block removed.
</objective>

## Phase Goal

**As a** trader on `/cot`, **I want to** filter pairs from the same row that summarizes each pair's signal, **so that** I read and filter in one place instead of scanning a redundant strip plus a separate tab row.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-signal-language/01-CONTEXT.md
@.planning/phases/01-signal-language/01-RESEARCH.md
@CLAUDE.md

# Plan 01-01 produced SignalBars — this plan consumes it
@.planning/phases/01-signal-language/01-01-PLAN.md
@src/app/(app)/cot/CotReports.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the merged pill row and wire it to the existing filter state</name>
  <files>src/app/(app)/cot/CotReports.tsx</files>
  <read_first>
    - src/app/(app)/cot/CotReports.tsx lines 348-369 (SummaryStrip function — layout template, shows `sig.icon` + pair + shortLabel + cotIndex), lines 404-439 (state: `selected`, `setSelected`, `pairs` memo = ['All', ...entries.map(e=>e.pair)], `visible`), lines 511-531 (the two render blocks to replace: SummaryStrip render at 511-512 and the filter-tab row at 514-531)
    - 01-RESEARCH.md Existing Patterns §5 (filter-tab selected-state classes: selected `bg-teal text-white`, unselected `bg-panel-2 text-ink-dim border border-line`) and Integration Points §3 (every entry has a valid signal even with zero history — EMPTY_COT_STATS.signal = "neutral", so SignalBars never gets an invalid key)
    - src/components/cot/SignalBars.tsx (from Plan 01-01 — `<SignalBars signal={entry.signal} size="sm" />`)
  </read_first>
  <action>
    Per PAGE-01 and the CONTEXT.md "merged pill row" decision: replace BOTH the SummaryStrip render (line 511-512: `{!loading && entries.length > 0 && <SummaryStrip entries={entries} />}`) and the entire pair-filter-tab block (lines 514-531) with ONE pill row.

    Render the row by iterating the existing `pairs` memo (`["All", ...entries.map(e => e.pair)]`) — do NOT introduce new state; reuse `selected`/`setSelected`/`visible` exactly as they are. For each `p` in `pairs`, render a `<button type="button" onClick={() => setSelected(p)}>`:
    - When `p === "All"`: text-only pill (no SignalBars — there is no entry to look up).
    - Otherwise: look up the matching entry (`entries.find(e => e.pair === p)`) and render `<SignalBars signal={entry.signal} size="sm" />` + the pair name. Import `SignalBars` from `@/components/cot/SignalBars` (add the import if Plan 01-01's card change did not already add it to this file).

    Selected state (Claude's discretion — use the COT-page-native, lower-diff option): selected pill `bg-teal text-white`; unselected `bg-panel-2 text-ink-dim border border-line`, matching the filter tabs being removed. Keep the container `flex items-center gap-1.5 flex-wrap mb-5` so it wraps on small screens (existing pattern). Preserve `active:scale-95 transition-all`.

    cotIndex decision (Claude's discretion, flagged in Research Risks #3): KEEP the `cotIndex` number in each per-pair pill as a muted trailing value (`tabular-nums text-[11px] opacity-75`), preserving the information density the old SummaryStrip provided; omit it on the "All" pill. This is a deliberate decision, not an oversight.

    Guard rendering the same way the removed blocks were guarded (`!loading`); the pill row should render once entries are loaded. Do NOT alter the header, the how-to-read strip, the color key (Plan 01-01), the cards grid, or any card internals.
  </action>
  <verify>
    <automated>npm run type-check && npm run lint</automated>
    <human-check>Load /cot: one pill row shows "All" plus each pair with its 3-bar icon and COT index; the old summary strip and the separate tab row are gone; tapping a pair pill filters the cards and that pill is clearly highlighted; tapping "All" shows every card; the row wraps in a narrow window.</human-check>
  </verify>
  <done>
    - A single pill row renders "All" + one pill per pair (pair name + SignalBars icon + cotIndex); selected pill is visually obvious (`bg-teal text-white`).
    - Tapping a pill updates `selected` and filters `visible`; "All" clears the filter.
    - The former SummaryStrip render and the filter-tab block are both gone; `npm run type-check` and `npm run lint` pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove the now-dead SummaryStrip function and confirm no orphan references</name>
  <files>src/app/(app)/cot/CotReports.tsx</files>
  <read_first>
    - src/app/(app)/cot/CotReports.tsx lines 348-369 (the SummaryStrip function definition to delete) and its former call site (removed in Task 1)
  </read_first>
  <action>
    Delete the `SummaryStrip` function definition (lines ~348-369) and its `// ── Summary strip ──` section separator comment now that Task 1 removed its only call site. Confirm no remaining reference to `SummaryStrip` exists in the file. Leave all other helper functions (CotCard, HistoryBadge, LoadingSkeleton) untouched. If removing SummaryStrip leaves any import unused (e.g. an icon or type only it referenced), remove that unused import too so lint stays clean — but do NOT remove imports still used by the pill row (SIGNAL_CFG is still needed for cotIndex lookups only if referenced; SignalBars is needed; Icon may still be used elsewhere).
  </action>
  <verify>
    <automated>npm run type-check && npm run lint && npm run build</automated>
    <human-check>/cot still renders correctly after the dead-code removal — pill row, cards, and color key all present; no console errors.</human-check>
  </verify>
  <done>
    - `SummaryStrip` no longer exists in CotReports.tsx and nothing references it.
    - No unused imports remain; `npm run type-check`, `npm run lint`, and `npm run build` all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Client-side rendering change. The only "input" is onClick selecting a pair from a closed, already-fetched list — not free text, no new endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Denial of Service | pill row render for empty/zero-history pairs | low | accept | Every entry always carries a valid signal (EMPTY_COT_STATS.signal = "neutral"), so SignalBars never receives an invalid key; `visible` filter logic is unchanged. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `npm run type-check`, `npm run lint`, and `npm run build` all pass.
- Visual on `/cot`: one merged pill row (All + per-pair pills with SignalBars); old summary strip and filter tabs gone; filtering works; row wraps on narrow widths.
- Grep confirms no remaining `SummaryStrip` reference in CotReports.tsx.
</verification>

<success_criteria>
- PAGE-01: the summary pill strip and pair filter tabs are replaced by ONE pill row; each pill shows pair + SignalBars icon and acts as the filter, with an "All" affordance.
</success_criteria>

<artifacts_this_phase_produces>
- Merged pill row inside `CotReports.tsx` (not a separate export); reuses existing `pairs`/`selected`/`setSelected`/`visible` state and consumes `SignalBars` from Plan 01-01.
- Removal: `SummaryStrip` function and the filter-tab block are deleted from `CotReports.tsx`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-signal-language/01-02-SUMMARY.md` when done.
</output>
