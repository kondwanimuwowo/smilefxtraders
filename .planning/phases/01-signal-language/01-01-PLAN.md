---
phase: 01-signal-language
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/cot/signalCfg.ts
  - src/components/cot/SignalBars.tsx
  - src/app/(app)/cot/CotReports.tsx
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03, SIG-04]
user_setup: []

must_haves:
  truths:
    - "Each COT card shows an icon-only 3-bar SignalBars icon (no text signal label) colored by the brightness-variant mapping"
    - "The 5 signal levels resolve their bar count and color from the single shared signalCfg.ts, not a forked lookup"
    - "A color key with 3 entries (Bearish / Neutral / Bullish) plus a USD-base-inverted entry renders above the cards on /cot"
  artifacts:
    - "src/components/cot/SignalBars.tsx (new standalone SVG component)"
    - "src/components/cot/signalCfg.ts (barCount field added to SignalCfg)"
    - "src/app/(app)/cot/CotReports.tsx (card badge replaced, color key added)"
  key_links:
    - "SignalBars reads SIGNAL_CFG[signal].barCount and SIGNAL_CFG[signal].strokeColor — the one source of truth"
    - "swap_horiz (already in ICON_REGISTRY) reused for the USD-base-inverted key entry — no registry change"
---

<objective>
Establish the signal visual language on the `/cot` overview: extend the shared `signalCfg.ts` with a bar-count datum, build a standalone `SignalBars` SVG component, swap the card's text signal badge for the icon-only SignalBars, and render a color key above the cards.

Purpose: The COT overview must speak one visual dialect — a 3-bar icon everyone can read at a glance, explained by a compact key — before the pill row (Plan 01-02) can reuse that same icon.
Output: `SignalBars.tsx` (new), `barCount` on `SignalCfg`, icon-only card signal, and a 4-entry color key.
</objective>

## Phase Goal

**As a** trader reading `/cot`, **I want to** see each pair's institutional bias as one consistent 3-bar icon with a key that explains it, **so that** I can read bias at a glance without decoding text badges.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-signal-language/01-CONTEXT.md
@.planning/phases/01-signal-language/01-RESEARCH.md
@CLAUDE.md

# Files this plan reads/edits directly
@src/components/cot/signalCfg.ts
@src/app/(app)/cot/CotReports.tsx
@src/app/(app)/cot/[pair]/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add barCount to signalCfg and build SignalBars, then swap it into the card badge</name>
  <files>src/components/cot/signalCfg.ts, src/components/cot/SignalBars.tsx, src/app/(app)/cot/CotReports.tsx</files>
  <read_first>
    - src/components/cot/signalCfg.ts (full — interface lines 7-16, SIGNAL_CFG record lines 18-24; strokeColor already holds the exact locked color per level)
    - src/lib/cot/types.ts (the CotSignal union that keys SIGNAL_CFG)
    - src/app/(app)/cot/CotReports.tsx lines 178, 203-226 (CotCard: `const sig = SIGNAL_CFG[entry.signal]` and the signal badge span at 212-216 — the SIG-04 removal target)
    - src/components/ui/Ring.tsx and src/components/ui/Sparkline.tsx (the established SVG pattern: raw `var(--x)` strings passed into fill/stroke attributes, NOT Tailwind color classNames)
    - 01-RESEARCH.md Existing Patterns §1, §5, §6 and Integration Points §1, §2; Risks/Gotchas #5 (SVG fill vs className color resolution)
  </read_first>
  <behavior>
    - SIGNAL_CFG resolves barCount: strong_bear=1, bear=1, neutral=2, bull=3, strong_bull=3.
    - SignalBars renders exactly 3 bar elements; for signal S the leftmost SIGNAL_CFG[S].barCount bars are filled with SIGNAL_CFG[S].strokeColor and the remaining bars use var(--track).
    - SignalBars carries an accessible name equal to SIGNAL_CFG[S].label (aria-label), since the visible text label is removed.
    - The CotCard header renders <SignalBars signal={entry.signal} /> in place of the former text badge, and no text signal label (sig.label pill) remains on the card.
  </behavior>
  <action>
    Per D (CONTEXT.md locked mapping) and SIG-01/SIG-02/SIG-04:

    1. In `src/components/cot/signalCfg.ts`: add one field `barCount: 1 | 2 | 3` to the `SignalCfg` interface (place it after `strokeColor`, matching the existing single-line comment style). Populate it on all 5 SIGNAL_CFG entries: `strong_bull` 3, `bull` 3, `neutral` 2, `bear` 1, `strong_bear` 1. Do NOT rename, remove, reorder, or make optional any existing field — 7 consumers across 5 files depend on the current shape (additive-only change). Keep every field plain serializable data (a plain number), because `src/lib/cot/notify.ts` imports this module server-side.

    2. Create `src/components/cot/SignalBars.tsx` as a standalone SVG React component — NOT an ICON_REGISTRY entry (registry is Heroicons keyed by Material names; SignalBars is domain-coupled to CotSignal, so it belongs in `components/cot/` next to signalCfg.ts, matching CotIndexDisplay's placement). Named export `SignalBars` (no default export, per module convention). Props interface `SignalBarsProps`: `{ signal: CotSignal; size?: "sm" | "md" | "lg"; className?: string }`. Internally `const cfg = SIGNAL_CFG[signal]`. Render an inline `<svg>` with three vertical `<rect>` bars of ascending height (short → tall) at equal widths/gaps (exact geometry is your discretion). Fill the leftmost `cfg.barCount` bars with `cfg.strokeColor` used directly as the SVG `fill` attribute (a raw `var(--x)` string — do NOT use a `text-*` className on `<rect>`; SVG shapes do not inherit Tailwind text color the way Heroicons do). Fill the remaining bars with `var(--track)`. Map `size` to px height (suggest sm≈14, md≈18, lg≈24; discretion). Set `aria-label={cfg.label}` and `role="img"` on the svg so the icon-only signal stays accessible. Forward `className`. Import `CotSignal` as a type-only import and `SIGNAL_CFG` from `./signalCfg`.

    3. In `src/app/(app)/cot/CotReports.tsx` CotCard (around lines 212-216): remove the text signal badge span (the `<span … sig.bgCls sig.textCls><Icon name={sig.icon}/>{sig.label}</span>`) and render `<SignalBars signal={entry.signal} size="md" />` in its place, keeping it inside the same header flex row so it sits beside the pair name. Import `SignalBars` from `@/components/cot/SignalBars`. Leave the `const sig = SIGNAL_CFG[entry.signal]` line only if `sig` is still referenced elsewhere in CotCard; if this was its sole use, remove the now-unused binding to keep type-check/lint clean. Do NOT touch the HistoryBadge or the USD-base badge on the card (Phase 2 scope).
  </action>
  <verify>
    <automated>npm run type-check && npm run lint</automated>
    <human-check>Load /cot: each card shows a 3-bar icon (1 coral bar for bearish pairs, 2 gold for neutral, 3 teal for bullish; strong variants brighter) and no text signal word beside the pair.</human-check>
  </verify>
  <done>
    - `SignalCfg` has a `barCount: 1 | 2 | 3` field populated 3/3/2/1/1 across the 5 levels; no existing field changed.
    - `src/components/cot/SignalBars.tsx` exists, exports `SignalBars`, resolves color+count from SIGNAL_CFG, and sets aria-label to the signal label.
    - CotCard renders SignalBars and no longer renders the text signal badge; `npm run type-check` and `npm run lint` pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the color key above the cards</name>
  <files>src/app/(app)/cot/CotReports.tsx</files>
  <read_first>
    - src/app/(app)/cot/CotReports.tsx lines 449-542 (the CotReports render tree: header, how-to-read strip at 501-509, SummaryStrip render at 511-512, cards grid at 533-542) — insert the key above the cards
    - src/app/(app)/cot/[pair]/page.tsx lines 296-334 (the existing detail-page color key: flex row, small swatches, muted "Color key" label, `flex-wrap`, and the `swap_horiz` USD-base entry at 311-315) — model the new key on this markup for cross-page consistency
    - 01-RESEARCH.md Existing Patterns §3 (swap_horiz already registered and used for USD-base) and Integration Points §4 (color key placement)
  </read_first>
  <action>
    Per SIG-03: add a color-key block in the CotReports render tree, above the cards grid (place it after the how-to-read strip; rendering it near where SummaryStrip currently sits is fine — Plan 01-02 removes SummaryStrip, not the key). Build it as a `flex flex-wrap` row of small entries modeled on the detail-page key at `cot/[pair]/page.tsx:296-334` (small SignalBars swatch + short label, muted `text-ink-mid`/`text-ink-dim`, `flex-wrap` so it wraps on narrow widths). Four entries, in order:

    1. `<SignalBars signal="strong_bear" size="sm" />` (or `"bear"`) labeled "Bearish" — the coral 1-bar swatch.
    2. `<SignalBars signal="neutral" size="sm" />` labeled "Neutral / Mixed" — the gold 2-bar swatch.
    3. `<SignalBars signal="bull" size="sm" />` (or `"strong_bull"`) labeled "Bullish" — the teal 3-bar swatch.
    4. `<Icon name="swap_horiz" size={12} />` labeled to explain USD-base-inverted pairs (reuse `swap_horiz` — it is already in ICON_REGISTRY and already carries this exact meaning on the detail page; do NOT add a registry entry).

    Prefix with a muted "Color key" label matching the detail page. Use only existing color tokens (`text-ink-mid`, `text-ink-dim`) and the SignalBars component for the colored swatches — never a hardcoded hex. Strong variants are represented by the brighter color of the base entries, NOT as separate key rows (locked in CONTEXT.md). If any hover-explain text is added on the swap_horiz entry, use the native `title=` + `cursor-help` idiom (Research: no Tooltip primitive exists).
  </action>
  <verify>
    <automated>npm run type-check && npm run lint</automated>
    <human-check>Load /cot: a "Color key" row appears above the cards with a coral 1-bar (Bearish), gold 2-bar (Neutral/Mixed), teal 3-bar (Bullish), and a swap icon explaining USD-base-inverted pairs; the row wraps on a narrow window.</human-check>
  </verify>
  <done>
    - A 4-entry color key (Bearish / Neutral / Bullish + USD-base-inverted swap_horiz) renders above the cards using SignalBars swatches and existing tokens.
    - `npm run type-check` and `npm run lint` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side presentational refactor of already-fetched, already-authorized COT data. No new route, endpoint, input, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | signalCfg.ts shared config | low | accept | Additive-only field change; `npm run type-check` across all 5 consumer files (incl. CotBiasPanel.tsx, notify.ts) proves no consumer breaks. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan — component is hand-written, reuses existing deps only. |
</threat_model>

<verification>
- `npm run type-check` passes (proves the additive `barCount` field type-checks against all 5 SIGNAL_CFG consumers).
- `npm run lint` passes (no unused `sig` binding, correct imports).
- `npm run build` succeeds.
- Visual on `/cot`: cards show icon-only 3-bar signals; color key renders above cards with the 4 entries.
</verification>

<success_criteria>
- SIG-01: card signal is a standalone SignalBars SVG (not an icon-registry entry).
- SIG-02: the 5 levels resolve bar count + color from signalCfg.ts (barCount added; strokeColor reused), not a fork.
- SIG-03: a 3-entry key + USD-base-inverted entry renders above the cards.
- SIG-04: the card shows the icon only — no text signal label.
</success_criteria>

<artifacts_this_phase_produces>
New/changed symbols other plans (and Plan 01-02) may consume:
- `SignalBars` — new named export, `src/components/cot/SignalBars.tsx`. Props `SignalBarsProps { signal: CotSignal; size?: "sm" | "md" | "lg"; className?: string }`. Renders a 3-bar SVG; resolves color+count from SIGNAL_CFG.
- `SignalCfg.barCount: 1 | 2 | 3` — new field on the existing interface in `src/components/cot/signalCfg.ts`; populated 3/3/2/1/1 for strong_bull/bull/neutral/bear/strong_bear.
- Color key block inside `CotReports.tsx` render tree (not a separate export).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-signal-language/01-01-SUMMARY.md` when done.
</output>
